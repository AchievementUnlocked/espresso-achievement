export class ControlerErrors {
    static readonly UnknownApiError = 'There was an error trying to execute the api request.';

    static readonly HandleCreateAchievementError = 'There was an error trying to handle the achievement creation event.';

    static readonly GetAchievementSummaryError = 'There was an error while tryng to get the achivement summaries.';
}
