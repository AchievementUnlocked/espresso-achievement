import { AchievementMediaFullDto } from './achevement-media-full.schema';

describe('AchevementMediaFullDto', () => {
  it('should be defined', () => {
    expect(new AchievementMediaFullDto()).toBeDefined();
  });
});
