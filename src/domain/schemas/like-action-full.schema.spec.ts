import { LikeActionFullSchema } from './like-action-full.schema';

describe('LikeActionFullSchema', () => {
  it('should be defined', () => {
    expect(new LikeActionFullSchema()).toBeDefined();
  });
});
