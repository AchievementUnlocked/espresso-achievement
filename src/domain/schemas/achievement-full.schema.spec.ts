import { AchievementFullSchema } from './achievement-full.schema';

describe('AchievementFullSchema', () => {
  it('should be defined', () => {
    expect(new AchievementFullSchema()).toBeDefined();
  });
});
