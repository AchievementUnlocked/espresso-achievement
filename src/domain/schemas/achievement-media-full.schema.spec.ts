import { AchievementMediaFullDto } from './achievement-media-full.schema';

describe('AchievementMediaFullDto', () => {
  it('should be defined', () => {
    expect(new AchievementMediaFullDto()).toBeDefined();
  });
});
