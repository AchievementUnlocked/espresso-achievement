import { UserprofileFullSchema } from './userprofile-full.schema';

describe('UserprofileFullSchema', () => {
  it('should be defined', () => {
    expect(new UserprofileFullSchema()).toBeDefined();
  });
});
