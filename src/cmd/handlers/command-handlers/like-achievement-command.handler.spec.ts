import { LikeAchievementCommandHandler } from './like-achievement-command.handler';

describe('LikeAchievementCommandHandler', () => {
  it('should be defined', () => {
    expect(new LikeAchievementCommandHandler()).toBeDefined();
  });
});
